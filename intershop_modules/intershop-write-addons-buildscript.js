// Generated by CoffeeScript 2.4.1
(function() {
  'use strict';
  var CND, FS, PATH, alert, badge, cast, check, debug, declare, declare_check, echo, help, info, is_sad, isa, jr, package_json, resolve_pkg, rpr, type_of, types, urge, validate, warn, whisper;

  //###########################################################################################################
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'INTERSHOP/INTERSHOP-WRITE-ADDONS-BUILDSCRIPT';

  debug = CND.get_logger('debug', badge);

  alert = CND.get_logger('alert', badge);

  whisper = CND.get_logger('whisper', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  info = CND.get_logger('info', badge);

  echo = CND.echo.bind(CND);

  //...........................................................................................................
  PATH = require('path');

  FS = require('fs');

  resolve_pkg = require('resolve-pkg');

  package_json = require(PATH.resolve(PATH.join(process.env.intershop_host_path, 'package.json')));

  //...........................................................................................................
  types = new (require('intertype')).Intertype();

  ({isa, validate, cast, check, declare, declare_check, is_sad, type_of} = types.export());

  //...........................................................................................................
  ({jr} = CND);

  require('cnd/lib/exception-handler');

  //###########################################################################################################
  if (module === require.main) {
    (() => {
      var abspath, addon, addon_id, addons, file, file_id, ref, relpath, target;
      addons = (require('./intershop-find-addons')).find_addons();
      for (addon_id in addons) {
        addon = addons[addon_id];
        echo();
        echo(`# ${'-'.repeat(108)}`);
        echo(`# Addon: ${addon_id}`);
        echo(`# ${addon.module.path}`);
        ref = addon.ipj.targets;
        for (file_id in ref) {
          file = ref[file_id];
          ({target, relpath, abspath} = file);
          switch (target) {
            case 'support':
              echo(`# skipping support file ${abspath}`);
              break;
            case 'rebuild':
              /* TAINT must escape critical characters */
              echo(`echo -e $orange$reverse $reset$orange'${abspath}'$reset`);
              echo(`postgres_unpaged -f ${abspath}`);
              break;
            default:
              echo(`# skipping ${abspath}`);
          }
        }
      }
      echo(`# ${'-'.repeat(108)}`);
      return echo("# (end of addons)");
    })();
  }

}).call(this);
